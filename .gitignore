packrat/lib*/
.Rproj.user
.Rprofile
*.Rproj
.Rhistory
*.DS_Store
inst/doc

# Local-only research data (bundled OSF data and paper materials, not distributed)
paper/

# Benchmark download and prediction cache
data-raw/benchmark/.cache/
data-raw/external-validation/.cache/

# Local review notes (not distributed)
audit/

# Rendered pkgdown site (published to the gh-pages branch)
docs/

# R CMD build/check artifacts
*.tar.gz
*.Rcheck/

# JOSS paper and Zenodo archive metadata (prepared locally, not yet published)
joss/
.zenodo.json
